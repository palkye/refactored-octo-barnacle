# Tetris Game

This repository contains a simple terminal-based Tetris game implemented in Python.

## Requirements

- Python 3
- A terminal that supports `curses`

## Running

Execute the game with:

```bash
python tetris.py
```

Use the arrow keys to move and rotate pieces. Press `q` to quit.
